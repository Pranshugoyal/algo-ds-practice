//Given a list of numbers and a number k, return whether any two numbers from the list add up to k.
//For example, given [10, 15, 3, 7, 7] and k of 17, return true since 10 + 7 is 17.
func pairExists(in array: [Int], sum k: Int) -> Bool











































//Given a sorted array numbers, remove the duplicates in-place such that each element appear only once and return the new length.
//Do not allocate extra space, you must do this by modifying the input array in-place with O(1) extra memory.
//[0, 1, 1, 3, 5, 5, 10] ---> [0, 1, 3, 5, 10], 5
func removeDuplicates(_ nums: inout [Int]) -> Int


//Given an array numbers of n integers where n > 1,
//return an array output such that output[i] is equal to the product of all the elements of numbers except nums[i].
func productExceptSelf(_ nums: [Int]) -> [Int]
